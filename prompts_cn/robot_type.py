SINGLE_ARM_ROBOT_PROMPT = """
该机器人为固定底座单机械臂操作机器人，由一个机械臂和一个夹爪（或灵巧手）组成，机器人底座保持静止，所有操作均通过机械臂完成。
机器人具有在工作空间内接近目标、调整姿态、抓取、提起、搬运、放置、推动、拉动、旋转、插入、拔出、打开、关闭、释放等常见操作能力。复杂任务通常由多个连续的基础操作组合完成。
分析视频时，应重点关注机器人执行任务的操作意图、与物体之间的交互过程、接触事件以及物体状态变化，而不是机械臂关节或末端执行器的细微运动轨迹。
理解 action_sequence 时，executor 固定使用 "single"；action 按时间顺序记录该机械臂实际执行的高层动作；object 记录该动作直接作用、抓取、移动、放置或朝向的对象，必须使用自然中文名称，如果没有明确对象则为空字符串 ""。
"""


BIMANUAL_ROBOT_PROMPT = """
该机器人为固定底座双机械臂操作机器人，由左右两个机械臂组成。两个机械臂既可以独立完成操作，也可以协同完成同一项任务。
机器人能够完成双臂协同接近目标、同步抓取、双手搬运、物体交接、固定物体、折叠、装配、开关容器、协同操作等复杂动作。许多任务需要两个机械臂分工合作，共同完成操作目标。
分析视频时，应将两个机械臂视为一个协同工作的整体，重点关注双臂之间的协作关系、物体交接、接触事件、物体状态变化以及任务目标的变化，而不是分别描述左右机械臂的运动轨迹。
理解 action_sequence 时，executor 使用 "left"、"right" 或 "both"；如果左右臂在同一时间段执行不同动作，应按时间顺序分别输出两条记录；如果两臂同步完成同一动作，可使用 "both"。action 记录对应执行主体的高层动作，object 记录该动作直接作用或朝向的对象，必须使用自然中文名称，如果没有明确对象则为空字符串 ""。
"""


MOBILE_MANIPULATOR_PROMPT = """
该机器人为移动操作机器人，由移动底盘和一个或多个机械臂组成。机器人不仅能够移动到目标位置，还能够利用机械臂完成各种操作任务，因此完整任务通常包含移动与操作两个阶段。
机器人能够完成导航、定位、接近目标、抓取、搬运、运输、放置、推动、拉动、开关门、开关抽屉、插入、取出等操作，并能够在不同位置之间移动完成长程操作任务。
分析视频时，应将机器人行为理解为移动与操作相结合的连续过程，区分导航阶段和操作阶段，重点关注机器人何时开始与物体交互、如何完成操作目标、物体状态如何发生变化，以及不同任务阶段之间的切换，而不是关注底盘运动或机械臂关节运动等底层控制细节。
理解 action_sequence 时，底盘移动或接近目标阶段的 executor 使用 "base"，机械臂操作阶段的 executor 使用 "arm"；action 按时间顺序记录导航、接近、抓取、搬运、放置等高层动作；object 对于导航动作表示目标位置或目标区域，对于操作动作表示被操作对象，必须使用自然中文名称，如果没有明确对象则为空字符串 ""。
"""


ROBOT_TYPE_PROMPTS = {
    "single_arm": SINGLE_ARM_ROBOT_PROMPT,
    "bimanual": BIMANUAL_ROBOT_PROMPT,
    "mobile_manipulator": MOBILE_MANIPULATOR_PROMPT,
}


def get_robot_type_prompt(robot_type: str) -> str:
    prompt = ROBOT_TYPE_PROMPTS.get(robot_type)
    if prompt:
        return prompt.strip()
    return (
        '当前 robot_type 为 "unknown"。不要猜测机器人类别，只根据视频中可见的动作完成标注；'
        'executor 使用 "unknown"，action 按时间顺序记录可见高层动作，object 使用自然中文名称记录明确对象或空字符串 ""。'
    )
